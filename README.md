# example-pkg

Generated with `uv init --package example_pkg`

## Development

Install the package for development with `uv sync --locked`

The project uses both ruff and ty from Astral. Installing the corresponding VSCode extensions will show linting and type-checking errors.
